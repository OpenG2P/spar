global:
  postgresqlHost: commons-postgresql
  sparDB: '{{ printf "%s" .Release.Name | replace "-" "_" }}'
  sparDBUser: '{{ printf "%s_user" .Release.Name | replace "-" "_" }}'
  sparDBSecret: '{{ .Release.Name }}'
  sparDBUserPasswordKey: '{{ .Release.Name }}-db-user'

  # Security & Authentication
  # Internal service name of Keymanager
  keymanagerInstallationName: commons-services-keymanager
  # Public (namespace-scoped) Keycloak hostname used by apps for the OIDC
  # issuer/token URL. The keycloak-init Job below talks to the same Keycloak
  # over its internal cluster URL.
  keycloakBaseUrl: 'https://keycloak.{{ .Release.Namespace }}.openg2p.org'
  keycloakRealm: 'staff'
  keycloakIssuerUrl: '{{ tpl .Values.global.keycloakBaseUrl $ }}/realms/{{ tpl .Values.global.keycloakRealm $ }}'
  # OIDC client ID — must match the clientId provisioned in keycloak-init.realms
  authClientId: 'openg2p-spar'
  # K8s Secret name holding the client password (created by keycloak-init).
  # By convention the Secret is named after the client ID.
  authClientSecret: '{{ tpl .Values.global.authClientId $ }}'
  # Key inside the secret containing the client password
  authClientSecretKey: 'client_secret'

  # ── JWS signature verification (openg2p-fastapi-common) ───────────────────
  # SPAR only VERIFIES partner signatures (it never signs), so there is no signing
  # key / .p12. Backend:
  #   "local"      -> in-process PyJWT; partner public certs verified from the
  #                   partner_keys DB table (seeded below). No Keymanager. Default.
  #   "keymanager" -> remote Keymanager service (the KEYMANAGER_* env in
  #                   sparMapperAPI.envVars).
  # SPAR verifies inbound partner signatures against public keys served by the
  # Partner Manager (PM) service (partner-mgmt) — no local key store, no Keymanager.
  sparCryptoBackend: partner-mgmt
  # In-cluster PM partner-api URL (unauthenticated key fetch). Read by
  # fastapi-common's PartnerMgmtKeyStore as GET {url}/keys/PARTNER_<mnemonic>.
  partnerManagementApiUrl: http://commons-services-pm-partner-api
  # RS256 only (asymmetric); "none" and HMAC (HS*) are always rejected.
  sparCryptoAllowedAlgorithms: "RS256"
  # Verify a partner JWS signature on every Mapper Partner API request. ON by
  # default (the trial Bridge signs its resolve calls); set false for an
  # unauthenticated demo.
  sparJwtAuthEnabled: true

  # Partner onboarding: partners (incl. the G2P Bridge as PARTNER_G2P_BRIDGE, and the
  # trial's test partners) are registered in the Partner Manager (PM) service — the
  # G2P Bridge chart's pm-register Job does this. SPAR seeds nothing; it fetches keys
  # from PM (see partnerManagementApiUrl above).

postgres-init:
  enabled: true
  postgresql:
    host: '{{ .Values.global.postgresqlHost }}'
    port: 5432
    # Hard coding. Same as one defined under Odoo
    existingSecret: commons-postgresql
    existingSecretPostgresPasswordKey: postgres-password
  databases:
    # Same as db in Odoo
    - name: '{{ tpl .Values.global.sparDB $ }}'
      user: '{{ tpl .Values.global.sparDBUser $ }}'
      secret: '{{ tpl .Values.global.sparDBSecret $ }}'
      secretUserPasswordKey: '{{ tpl .Values.global.sparDBUserPasswordKey $ }}'

# Re-sync the SPAR DB role password to its Secret before the app starts. postgres-init
# only CREATEs a missing role (never updates an existing role's password), so on a
# persistent commons-postgresql the role password can drift from the regenerated Secret
# and the app's migrate fails to authenticate. This pre-upgrade hook fixes that
# (idempotent; no-op when the role/secret is absent). See templates/db-password-sync.
dbPasswordSync:
  enabled: true
  # Postgres superuser credentials (same source postgres-init uses).
  superuserSecret: commons-postgresql
  superuserPasswordKey: postgres-password
  image:
    repository: jbergknoff/postgresql-client
    tag: latest
    pullPolicy: IfNotPresent

# Provisions the SPAR OIDC client in Keycloak. The Job is idempotent: it
# creates the realm if absent and adds the client, leaving anything that
# already exists untouched. For each confidential client it also creates a
# K8s Secret named after the clientId (key: client_secret) with an
# auto-generated password — this is the Secret the mapper API consumes via
# global.authClientSecret / global.authClientSecretKey above.
# NOTE: realm/clientId reference the global.* values (the only values visible
# to a subchart) so they are defined once and never duplicated here.
# OFF by default: the openg2p-spar OIDC client is only used to authenticate the
# Mapper API to MOSIP Keymanager, which the default local crypto backend does not
# use. Enable only if you switch global.sparCryptoBackend to the legacy "keymanager".
keycloak-init:
  enabled: false
  keycloak:
    # Internal cluster URL of the Keycloak installed in this namespace.
    url: 'http://commons-keycloak:80'
    user: 'admin'
    password: ''
    # The Keycloak chart's auto-created admin secret (<commons-release>-keycloak).
    existingSecret: 'commons-keycloak'
    existingSecretKey: 'admin-password'
  realms:
    '{{ tpl .Values.global.keycloakRealm $ }}':
      clients:
        - clientId: '{{ tpl .Values.global.authClientId $ }}'
          name: OpenG2P SPAR
          # Confidential service-to-service client (OAuth2 client_credentials):
          # the mapper API exchanges client id + secret for a token. It has no
          # browser login flow, so no redirect URIs / web origins are needed.
          redirectUris: []
          webOrigins: []

# Seeds shared SPAR reference data (the `strategy` table) at install/upgrade via
# a post-install/post-upgrade Job. Seeding is ADDITIVE and IDEMPOTENT: each row
# is inserted by its pinned `id` with `ON CONFLICT (id) DO NOTHING`, so existing
# rows are never modified or deleted. To add a strategy later, append a new entry
# with a new `id` and run `helm upgrade` — nothing else changes.
#
# A Strategy defines how a Financial Address (FA) — or an ID — is serialized and
# parsed:
#   construct_strategy   : a Python format string, e.g. "{bank_code}-{acc}"
#   deconstruct_strategy : a regex with named groups, e.g. "(?P<bank_code>.*)-(?P<acc>.*)"
# The keys must match the FA/ID field names. The link/update API references a
# strategy by its integer `id` (fa.strategy_id), so ids must stay stable.
seedData:
  enabled: true
  # Image providing psql / pg_isready for the seed Job.
  image: jbergknoff/postgresql-client
  strategies:
    - id: 1
      description: Key Cloak
      strategy_type: ID
      construct_strategy: 'token:{sub}@nationalId'
      deconstruct_strategy: '^token:(?P<sub>.[^.]*)@nationalId$'
    - id: 2
      description: Bank
      strategy_type: FA
      construct_strategy: 'account_number:{account_number}.branch_name:{branch_name}.branch_code:{branch_code}.bank_name:{bank_name}.bank_code:{bank_code}.fa_type:{fa_type}'
      deconstruct_strategy: '^account_number:(?P<account_number>.*)\.branch_name:(?P<branch_name>.*)\.branch_code:(?P<branch_code>.*)\.bank_name:(?P<bank_name>.*)\.bank_code:(?P<bank_code>.*)\.fa_type:(?P<fa_type>.*)$'
    - id: 3
      description: Email
      strategy_type: FA
      construct_strategy: 'email_address:{email_address}.wallet_provider_name:{wallet_provider_name}.wallet_provider_code:{wallet_provider_code}.fa_type:{fa_type}'
      deconstruct_strategy: '^email_address:(?P<email_address>.*)\.wallet_provider_name:(?P<wallet_provider_name>.*)\.wallet_provider_code:(?P<wallet_provider_code>.*)\.fa_type:(?P<fa_type>.*)$'
    - id: 4
      description: Phone
      strategy_type: FA
      construct_strategy: 'mobile_number:{mobile_number}.wallet_provider_name:{wallet_provider_name}.wallet_provider_code:{wallet_provider_code}.fa_type:{fa_type}'
      deconstruct_strategy: '^mobile_number:(?P<mobile_number>.*)\.wallet_provider_name:(?P<wallet_provider_name>.*)\.wallet_provider_code:(?P<wallet_provider_code>.*)\.fa_type:(?P<fa_type>.*)$'
    # Bank FA serialized in the exact shape the G2P Bridge expects
    # (G2P_BRIDGE_CELERY_WORKERS_BANK_FA_DECONSTRUCT_STRATEGY):
    #   account_number:<a>.branch_code:<b>.bank_code:<c>.mobile_number:.email_address:.fa_type:BANK_ACCOUNT
    # mobile_number / email_address are emitted empty (a bank FA has neither), and
    # fa_type is hardcoded to BANK_ACCOUNT because the Bridge/Example-Bank route on
    # the literal "BANK_ACCOUNT" (SPAR's FA provider enum is "BANK").
    - id: 5
      description: Bank (G2P Bridge)
      strategy_type: FA
      construct_strategy: 'account_number:{account_number}.branch_code:{branch_code}.bank_code:{bank_code}.mobile_number:.email_address:.fa_type:BANK_ACCOUNT'
      deconstruct_strategy: '^account_number:(?P<account_number>.*)\.branch_code:(?P<branch_code>.*)\.bank_code:(?P<bank_code>.*)\.mobile_number:(?P<mobile_number>.*)\.email_address:(?P<email_address>.*)\.fa_type:(?P<fa_type>.*)$'

sparMapperAPI:
  enabled: true
  sparHostname: spar.trial.openg2p.org
  postgresql:
    enabled: false
  replicaCount: 1
  image:
    repository: registry.gitlab.com/openg2p/spar/spar/mapper-partner-api
    pullPolicy: Always
    tag: "develop"
  imagePullSecrets: []
  nameOverride: ""
  fullnameOverride: ""
  envVars:
    SPAR_MAPPER_PARTNER_API_DB_HOSTNAME: '{{ .Values.global.postgresqlHost }}'
    # Keymanager (request signature verify / sign). The app's KeymanagerCryptoHelper
    # reads these KEYMANAGER_* settings and appends /jwtVerify and /jwtSign to the
    # base URL itself, so this must be the base path (no /jwtVerify suffix).
    SPAR_MAPPER_PARTNER_API_KEYMANAGER_API_BASE_URL: 'http://{{ .Values.global.keymanagerInstallationName }}/v1/keymanager'
    SPAR_MAPPER_PARTNER_API_KEYMANAGER_SIGN_APP_ID: 'OPENG2P_SPAR'
    # Keymanager is reached over its in-cluster Service, which does not enforce OAuth,
    # so auth is disabled. To enable, set this 'true'; AUTH_URL / CLIENT_ID / the
    # CLIENT_SECRET in envVarsFrom below are already wired for that case.
    SPAR_MAPPER_PARTNER_API_KEYMANAGER_AUTH_ENABLED: 'false'
    # Partner request Signature JWT verification — ON by default (the trial Bridge
    # signs its resolve calls). Set sparJwtAuthEnabled=false for an unsigned demo.
    SPAR_MAPPER_PARTNER_API_JWT_AUTH_ENABLED: '{{ .Values.global.sparJwtAuthEnabled }}'
    # Crypto backend = partner-mgmt: verify partner signatures against public keys
    # fetched from the Partner Manager (PM) service.
    SPAR_MAPPER_PARTNER_API_CRYPTO_BACKEND: '{{ .Values.global.sparCryptoBackend }}'
    SPAR_MAPPER_PARTNER_API_CRYPTO_ALLOWED_ALGORITHMS: '{{ .Values.global.sparCryptoAllowedAlgorithms }}'
    # PM partner-api (unauthenticated key fetch): GET {url}/keys/PARTNER_<mnemonic>.
    # Partners (incl. PARTNER_G2P_BRIDGE) are onboarded in PM by the G2P Bridge chart.
    SPAR_MAPPER_PARTNER_API_PARTNER_MGMT_API_URL: '{{ tpl .Values.global.partnerManagementApiUrl $ }}'
    SPAR_MAPPER_PARTNER_API_KEYMANAGER_AUTH_URL: '{{ tpl .Values.global.keycloakIssuerUrl $ }}/protocol/openid-connect/token'
    SPAR_MAPPER_PARTNER_API_KEYMANAGER_AUTH_CLIENT_ID: '{{ tpl .Values.global.authClientId $ }}'
    SPAR_MAPPER_PARTNER_API_DB_DBNAME: '{{ tpl .Values.global.sparDB $ }}'
    SPAR_MAPPER_PARTNER_API_DB_USERNAME: '{{ tpl .Values.global.sparDBUser $ }}'
    SPAR_MAPPER_PARTNER_API_OPENAPI_ROOT_PATH: "/api/mapper"
  envVarsFrom:
    SPAR_MAPPER_PARTNER_API_DB_PASSWORD:
      secretKeyRef:
        name: '{{ tpl .Values.global.sparDBSecret $ }}'
        key:  '{{ tpl .Values.global.sparDBUserPasswordKey $ }}'
    # optional: only exists when keycloak-init runs (legacy keymanager backend).
    # With the default partner-mgmt backend keycloak-init is off and this secret is
    # absent — mark optional so the container still starts (the value is unused).
    SPAR_MAPPER_PARTNER_API_KEYMANAGER_AUTH_CLIENT_SECRET:
      secretKeyRef:
        name: '{{ tpl .Values.global.authClientSecret $ }}'
        key: '{{ tpl .Values.global.authClientSecretKey $ }}'
        optional: true

  containerPort: 8000
  service:
    type: ClusterIP
    port: 80
  resources: {}
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 1
    targetCPUUtilizationPercentage: 80
  readinessProbe:
    httpGet:
      path: /ping
      port: 8000
    failureThreshold: 20
    periodSeconds: 30
    timeoutSeconds: 10
    initialDelaySeconds: 30
  livenessProbe:
    httpGet:
      path: /ping
      port: 8000
    initialDelaySeconds: 15
    periodSeconds: 60
    failureThreshold: 5
  startupProbe:
    enabled: true
    httpGet:
      path: /ping
      port: 8000
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 30
    successThreshold: 1
  serviceAccount:
    create: false
    annotations: {}
    name: ""
  podAnnotations:
    helm.sh/revision: '{{ .Release.Revision }}'
  podSecurityContext: {}
  containerSecurityContext: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}
  updateStrategy:
    type: RollingUpdate
  command: []
  args: []
  postgresCheckerInit:
    enabled: true
    image: jbergknoff/postgresql-client
    command:
    - 'sh'
    - '-c'
    - 'until pg_isready -U ${SPAR_MAPPER_PARTNER_API_DB_USERNAME} -h ${SPAR_MAPPER_PARTNER_API_DB_HOSTNAME} -p ${SPAR_MAPPER_PARTNER_API_DB_PORT:-5432} -d ${SPAR_MAPPER_PARTNER_API_DB_DBNAME:-spar_db}; do sleep 3; done'
  ingress:
    enabled: false
    className: ""
    annotations:
      nginx.ingress.kubernetes.io/rewrite-target: /$2
      kubernetes.io/ingress.class: nginx
    hosts:
      - host: ""
        paths:
          - path: /api/mapper(/|$)(.*)
            pathType: ImplementationSpecific
    tls: []
  istio:
    enabled: true
    virtualservice:
      enabled: true
      host: ""
      gateway: "internal"
      destination: '{{ include "common.names.fullname" $ }}-mapper-api'
      destinationPort: '{{ .Values.service.port }}'
      prefix: /api/mapper/
      rewriteUri: "/"
    gateway:
      enabled: false
      host: ""
      ingressController:
        istio: ingressgateway
      httpTlsRedirect: true
      httpEnabled: true
      httpsEnabled: false
      tls:
        mode: SIMPLE
        credentialName: ""

benePortalAPI:
  enabled: true
  benePortalHostname: beneficiary.trial.openg2p.org
  replicaCount: 1
  image:
    repository: registry.gitlab.com/openg2p/spar/spar/bene-portal-api
    pullPolicy: Always
    tag: "develop"
  imagePullSecrets: []
  nameOverride: ""
  fullnameOverride: ""
  envVars:
    G2P_SPAR_BENE_PORTAL_API_DB_HOSTNAME: '{{ .Values.global.postgresqlHost }}'
    G2P_SPAR_BENE_PORTAL_API_DB_PORT: '5432'
    G2P_SPAR_BENE_PORTAL_API_DB_DBNAME: '{{ tpl .Values.global.sparDB $ }}'
    G2P_SPAR_BENE_PORTAL_API_DB_USERNAME: '{{ tpl .Values.global.sparDBUser $ }}'
  envVarsFrom:
    G2P_SPAR_BENE_PORTAL_API_DB_PASSWORD:
      secretKeyRef:
        name: '{{ tpl .Values.global.sparDBSecret $ }}'
        key: '{{ tpl .Values.global.sparDBUserPasswordKey $ }}'
  containerPort: 8000
  service:
    type: ClusterIP
    port: 80
  resources: {}
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 1
    targetCPUUtilizationPercentage: 80
  readinessProbe:
    httpGet:
      path: /ping
      port: 8000
    failureThreshold: 20
    periodSeconds: 30
    timeoutSeconds: 10
    initialDelaySeconds: 30
  livenessProbe:
    httpGet:
      path: /ping
      port: 8000
    initialDelaySeconds: 15
    periodSeconds: 60
    failureThreshold: 5
  startupProbe:
    enabled: true
    httpGet:
      path: /ping
      port: 8000
    initialDelaySeconds: 0
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 30
    successThreshold: 1
  serviceAccount:
    create: false
    annotations: {}
    name: ""
  podAnnotations:
    helm.sh/revision: '{{ .Release.Revision }}'
  podSecurityContext: {}
  containerSecurityContext: {}
  nodeSelector: {}
  tolerations: []
  affinity: {}
  updateStrategy:
    type: RollingUpdate
  command: []
  args: []
  postgresCheckerInit:
    enabled: true
    image: jbergknoff/postgresql-client
    command:
    - 'sh'
    - '-c'
    - 'until pg_isready -U ${G2P_SPAR_BENE_PORTAL_API_DB_USERNAME} -h ${G2P_SPAR_BENE_PORTAL_API_DB_HOSTNAME} -p ${G2P_SPAR_BENE_PORTAL_API_DB_PORT:-5432} -d ${G2P_SPAR_BENE_PORTAL_API_DB_DBNAME:-spardb}; do sleep 3; done'
  ingress:
    enabled: false
    className: ""
    annotations:
      nginx.ingress.kubernetes.io/rewrite-target: /$2
      kubernetes.io/ingress.class: nginx
    hosts:
      - host: ""
        paths:
          - path: /api/bene-portal(/|$)(.*)
            pathType: ImplementationSpecific
    tls: []
  istio:
    enabled: true
    virtualservice:
      enabled: true
      host: ""
      gateway: "internal"
      destination: '{{ include "common.names.fullname" $ }}-bene-portal-api'
      destinationPort: '{{ .Values.service.port }}'
      prefix: /api/bene-portal/
      rewriteUri: "/"
    gateway:
      enabled: false
      host: ""
      ingressController:
        istio: ingressgateway
      httpTlsRedirect: true
      httpEnabled: true
      httpsEnabled: false
      tls:
        mode: SIMPLE
        credentialName: ""
