{{- $values := index .Values "benePortalAPI" -}}
{{- $global := .Values.global -}}
{{- if $values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "beneportal.serviceAccountName" . }}
  labels:
    {{- include "beneportal.labels" . | nindent 4 }}
  {{- with $values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
